White a fuction that takes in an array of integers and returns a sorted aray of the three largest integers in the input array. Not that the functin should return duplicate integers if necessary; for example it should return [10,10,12] for an input array of [10,5,9,10,12]

